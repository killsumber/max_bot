# Тестирование
pytest>=8.0
pytest-django>=4.9          # интеграция pytest + Django
pytest-cov>=5.0             # покрытие кода
pytest-mock>=3.14           # моки (заменяют unittest.mock)
requests-mock>=1.11         # мокать HTTP-запросы (для бота/API)

# Линтеры и форматирование (самые современные на 2025)
ruff>=0.5                   # замена flake8 + isort +部分pylint (быстро и мощно)
black>=24.0                 # форматирование кода
mypy>=1.10                  # статическая типизация (опционально, но очень полезно)

# Отладка и удобство
ipython>=8.20               # продвинутая REPL
django-debug-toolbar>=4.4   # панель отладки в браузере (только DEBUG=True!)

# Документация (если хочешь генерить)
sphinx>=7.3
sphinx-rtd-theme>=2.0

# CI/CD и проверки
pre-commit>=3.7             # хуки перед коммитом (например, прогон black/ruff)